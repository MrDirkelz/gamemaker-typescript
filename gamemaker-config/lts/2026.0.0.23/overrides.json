{
  "schemaVersion": 1,
  "runtimeVersion": "2026.0.0.23",
  "functions": {
    "array_create": "Correlate the returned array element with the supplied value.",
    "ds_grid_create": "Create a generic grid handle.",
    "ds_grid_get": "Return the grid element type.",
    "ds_grid_set": "Require the grid element type.",
    "ds_list_add": "Require values matching the generic list element type.",
    "ds_list_create": "Create a generic list handle.",
    "ds_list_find_value": "Return the generic list element type.",
    "ds_list_set": "Require values matching the generic list element type.",
    "ds_map_add": "Correlate map key and value types.",
    "ds_map_create": "Create a generic map handle.",
    "ds_map_find_value": "Return the generic map value type.",
    "ds_map_keys_to_array": "Return the generic map key type.",
    "ds_map_set": "Correlate map key and value types.",
    "ds_map_values_to_array": "Return the generic map value type.",
    "ds_priority_add": "Require values matching the generic priority element type.",
    "ds_priority_create": "Create a generic priority handle.",
    "ds_priority_delete_max": "Return the generic priority element type.",
    "ds_priority_delete_min": "Return the generic priority element type.",
    "ds_priority_find_max": "Return the generic priority element type.",
    "ds_priority_find_min": "Return the generic priority element type.",
    "ds_queue_create": "Create a generic queue handle.",
    "ds_queue_dequeue": "Return the generic queue element type.",
    "ds_queue_enqueue": "Require values matching the generic queue element type.",
    "ds_queue_head": "Return the generic queue element type.",
    "ds_queue_tail": "Return the generic queue element type.",
    "ds_stack_create": "Create a generic stack handle.",
    "ds_stack_pop": "Return the generic stack element type.",
    "ds_stack_push": "Require values matching the generic stack element type.",
    "ds_stack_top": "Return the generic stack element type.",
    "collision_circle": "Correlate object, instance, tile-map, and array targets with the collision result.",
    "collision_circle_list": "Correlate the destination list with collision targets.",
    "collision_ellipse": "Correlate object, instance, tile-map, and array targets with the collision result.",
    "collision_ellipse_list": "Correlate the destination list with collision targets.",
    "collision_line": "Correlate object, instance, tile-map, and array targets with the collision result.",
    "collision_line_list": "Correlate the destination list with collision targets.",
    "collision_point": "Correlate object, instance, tile-map, and array targets with the collision result.",
    "collision_point_list": "Correlate the destination list with collision targets.",
    "collision_rectangle": "Correlate object, instance, tile-map, and array targets with the collision result.",
    "collision_rectangle_list": "Correlate the destination list with collision targets.",
    "instance_create_depth": "Return the concrete live instance and type the creation struct.",
    "instance_create_layer": "Return the concrete live instance and type the creation struct.",
    "instance_find": "Correlate the object asset with the live instance result.",
    "instance_furthest": "Correlate the object asset with the live instance result.",
    "instance_id_get": "The argument is a numeric active-instance index, not an instance handle.",
    "instance_nearest": "Correlate the object asset with the live instance result.",
    "instance_place": "Correlate the supplied targets with the result.",
    "instance_place_list": "Correlate the destination list with supplied targets.",
    "instance_position": "Correlate the supplied targets with the result.",
    "instance_position_list": "Correlate the destination list with supplied targets.",
    "move_and_collide": "Correlate the returned collision array with supplied targets."
  },
  "globals": {
    "sprite_index": "Asset.GMSprite | -1"
  },
  "additionalConstants": {
    "audiogroup_default": "Asset.GMAudioGroup"
  }
}
